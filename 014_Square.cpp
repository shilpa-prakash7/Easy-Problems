#include <iostream>

using namespace std;

int main()
{
    int num;    //Declaring variable num

    cout << "Enter a number to be squared: ";
    cin >> num;   //Gets the number to be squared from user

    cout << "\nSquare of " << num << " = " << num*num;  //Prints the square using num*num
}